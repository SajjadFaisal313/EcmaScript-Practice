const fencingForAny = (edges, numOfWires, rate) => {
let sum = 0;
for (let i = 0; i < edges.length; i++) {
    sum = sum = edges[i]
}
return sum* numOfWires* rate;
};

console.log(`Cost for fencing is ${fencingForAny([45,45,23,68,15,78,41],7,12)}/-`)

